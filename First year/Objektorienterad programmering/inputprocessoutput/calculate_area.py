width = float(input("Type in the width: "))
height = float(input("Type in the height: "))

area = width * height

print(f"The width is {width} and the height is {height} which makes the area {area:.2f}")